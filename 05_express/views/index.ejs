<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>실습. post로 정보 받기</title>
  </head>
  <body>
    <form action="/postForm" method="post" target="_blank">
      이름 <input type="text" name="name" />
      <br />
      <fieldset>
        <legend>성별</legend>
        <label for="male"
          ><input
            type="radio"
            id="male"
            name="gender"
            value="남자"
          />남자</label
        >

        <label for="female">
          <input
            type="radio"
            id="female"
            name="gender"
            value="여자"
          />여자</label
        >
      </fieldset>
      <br />
      <fieldset>
        <legend>생년월일</legend>
        <select name="year">
          <%for(i=1985;i<2025;i++){%>
          <option value="<%=i%>"><%=i%></option>
          <%}%></select
        >년
        <select name="month">
          <%for(i=1;i<13;i++){%>
          <option value="<%=i%>"><%=i%></option>
          <%}%></select
        >월
        <select name="date">
          <%for(i=1;i<32;i++){%>
          <option value="<%=i%>"><%=i%></option>
          <%}%></select
        >일
      </fieldset>
      <br />
      <fieldset>
        <legend>관심사</legend>
        <label for="trip"
          ><input type="checkbox" name="favorite" id="trip" value="여행" />
          여행</label
        >
        <label for="fashion"
          ><input type="checkbox" name="favorite" id="fashion" value="패션" />
          패션</label
        >
        <label for="food"
          ><input type="checkbox" name="favorite" id="food" value="음식" />
          음식</label
        >
      </fieldset>
      <br />
      <hr />
      <h2>POST 정보</h2>
      <fieldset>
        <legend>선택</legend>
        <label for="color"
          >좋아하는 색깔<input type="color" id="color" name="color"
        /></label>
        <label for="num"
          >좋아하는 숫자<input type="text" id="num" name="num"
        /></label>
      </fieldset>
      <button>회원가입</button>
    </form>
  </body>
</html>
